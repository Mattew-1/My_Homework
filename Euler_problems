def tack3(n):
    '''Условие: Простые делители числа 13195 - это 5, 7, 13 и 29.
                Каков самый большой делитель числа 600851475143, являющийся простым числом?

    O Функции: Найдёт и вернёт наибольший простой делитель числа.'''

    res = []
    d = 2
    while d * d <= n: # по свойству простых чисел
        if n % d == 0:
            res.append(d)
            n //= d
        else:
            d += 1
    if n > 1:
        res.append(n)
    return max(res)

def tack4():
    '''Условие: Число-палиндром с обеих сторон (справа налево и слева направо) читается одинаково. 
                Самое большое число-палиндром, полученное умножением двух двузначных чисел – 9009 = 91 × 99.
                Найдите самый большой палиндром, полученный умножением двух трехзначных чисел.
                
    О функции: Найдёт и вернёт самый большьй палиндром получившийся перемножением двух трехзначных чисел.'''

    def is_pal(c):
        ''' Вернёт True if число палиндром else False'''
        return int(str(c)[::-1]) == c

    maxpal = 0
    for a in range(999, 99, -1):
       for b in range(a, 99, -1):
           prod = a * b
           if is_pal(prod) and prod > maxpal:
               maxpal = prod
    return maxpal

if __name__ == "__main__":
    #>>> 600851475143
    num = input("num: ")
    print(f"НПД: {tack3(int(num))}")#3
    
    print(f"Наибольший палиндром: {tack4()}")#4
